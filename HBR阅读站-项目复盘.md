# 项目复盘：我用 AI 助手搭了一个「哈佛商业评论经典阅读站」

> 本文是一份项目复盘，也可直接作为公众号首篇内容的底稿。
> 项目成品：https://ac1232373aa64253bb48bf611c06d689.app.codebuddy.work

---

## 一、为什么做这个？

**1. 个人痛点：经典散落，没有「一个能读的入口」**
哈佛商业评论（HBR）沉淀了过去 60 年最重要的管理思想——《营销短视》《什么是战略》《蓝海战略》《创新者的窘境》……但原文散落在 hbr.org，英文门槛高、找原文链接麻烦，也没有一个「中文摘要 + 原文直链」一体的阅读入口。

**2. 自我策展：给自己做一个可持续的阅读站**
最初的诉求很简单——把经典文章收集成一个网页，方便自己系统化重读。后来升级为「动态更新的阅读网站」：不只是静态陈列，而是能持续新增、长期维护的私人图书馆。

**3. 天然适配「公众号 IP」**
这个项目的本质就是**内容策展**：挑篇目、写原创中文摘要、配原文链接。这恰恰是最适合做成个人 IP 的形态——你不需要原创理论，但需要「判断力 + 持续输出 + 可信赖的整理」。HBR 经典解读，是一个可以长期做、越做越有壁垒的公众号垂直方向。

**4. 一条底线：尊重版权**
从第一天就定下规则——**只做原创中文摘要 + 外链到 HBR 官方，绝不搬运全文**。这既保护了自己，也让内容能放心公开传播。

---

## 二、怎么做的？

**技术选型：纯静态站（HTML / CSS / JS，零构建、零后端）**
- 不需要服务器，不需要数据库，文件往静态托管一丢就能跑。
- 版权安全：全文留在 HBR 官网，本站只放摘要和外链。
- 可公网、可书签、可手机直接打开。

**数据驱动：一个数组喂全场**
- `data.js` 里维护一个 `ARTICLES` 数组，每条含：中文标题、英文标题、作者、年份、分类、原创摘要、标签，以及（后加的）官方原文直链 `url`。
- `app.js` 负责渲染：搜索、分类筛选 chips、按年份/标题排序、点击弹出的详情卡。

**视觉与体验**
- 暗色「编辑风」主题，HBR 红（`#c8102e`）点缀，衬线标题，读起来像一本管理学期刊。
- 「新」角标系统：哪些是新加的，一眼可见。

**内容扩张：从 1960–2010 到 1960–2026**
- 初版覆盖 21 篇（1960–2010）。
- 用户指出断层后，用 WebFetch 逐篇核对 HBR 官方检索页，**确认真实篇目 / 作者 / 发表日期**，新增 13 篇（2021×1、2022×2、2023×1、2024×3、2025×3、2026×3），总数 21 → **34 篇**，年代跨度补齐到 1960–2026，每年都有覆盖。
- 新增分类：人才管理 / ESG 与可持续 / AI 与自动化 / 组织管理。

**升级为「动态阅读站」**
- 部署到 CloudStudio，生成公网地址，手机直接可开。
- 建立「双周策展」自动化：每两周周五，AI 自动策展 3–5 篇真实 HBR 文章、更新元数据、重新部署、汇报新链接。站点从「做完」变成「自己会长」。

---

## 三、遇到什么问题？

**问题 1：年代断层**
原站只覆盖到 2010 年，而近几年的管理学思潮——大辞职、ESG 之争、生成式 AI 对组织的影响——完全缺失。用户一眼就发现了这个硬伤。

**问题 2：「阅读原文」跳错地方（最直接的用户投诉）**
用户截图反馈：点详情里的「阅读原文（HBR 官方）」，跳到的不是文章页，而是 **HBR 的搜索结果列表**。体验直接崩塌——点了「原文」却进了检索页。

**问题 3：链接核实难**
- 部分篇目是**书**（如《执行》Execution），HBR 上没有独立文章页，不能硬塞一个不存在的直链。
- 几本「派生书」需要映射到内容对应的最相关 HBR 文章（如《从优秀到卓越》→ Level 5 Leadership 一文），映射错了就会 404。
- 猜测 URL 是大忌——HBR 的 slug 命名不规则，猜错必 404。

**问题 4：版权红线**
不能复制全文，只能做摘要 + 外链。这反而逼出了清晰的定位：本站是「导览」而非「转载」。

---

## 四、调试过程（踩坑与修复）

**① 补近年文章（2021–2026）**
- 用 WebFetch 拉 HBR 检索页，逐篇确认真实标题、作者、日期后再写入 `data.js`。
- 原则：**不靠记忆编，靠官方页核**。校验用 Node 编译跑一遍，确认字段齐全、外链格式正确。

**② 修「阅读原文」直链（核心修复）**
根因：旧逻辑一律调用 `buildHbrLink(enTitle)`，生成的是 `hbr.org/search?...` 检索页链接。
修复思路：给每篇文章补一个**已核实的文章详情页直链** `url`，前端优先用它。

具体踩坑：
- **写注入脚本翻车**：inline `node -e` 被 shell 转义搞坏（两次）。改法：写成独立的 `_validate.js` 文件再跑，不再用一行命令。
- **`ARTICLES is not defined`**：`data.js` 里 `const ARTICLES` 是模块局部变量，`require` 不到。改法：在源码字符串末尾手动 `append` 一行 `module.exports = { ARTICLES, META, buildHbrLink };` 再编译校验。
- **Kotter《推动变革》猜错 URL**：猜的 `/1995/01/leading-change` 直接 404。核实后正确 slug 是 `/1995/03/leading-change-why-transformation-efforts-fail-2`。
- **《执行》是书**：无独立文章页，保留搜索回退（不硬编直链）。
- **前端改动**：`app.js` 把 `const link = buildHbrLink(a.enTitle);` 改为
  ```js
  // 优先使用已核实的文章直链；未配置直链时回退到 HBR 官方检索
  const link = a.url || buildHbrLink(a.enTitle);
  ```
- **重新部署**：复用原 CloudStudio 沙箱，公网链接**保持不变**，用户无需换书签。

校验结果：34 篇文章，33 条核实直链 + 1 条（《执行》）搜索回退，无畸形 URL，`app.js` 语法检查通过。

---

## 五、最后的交付结果

**公网成品**
- 地址：https://ac1232373aa64253bb48bf611c06d689.app.codebuddy.work
- 手机浏览器直接打开、可收藏为书签、无需登录。

**内容**
- **34 篇** HBR 经典，年代 **1960–2026** 逐年覆盖，含 2026 年最新 AI 系列（《AI 领导力要务》《AI 搞坏了招聘》《别让 AI 废稿污染公司流程》等）。
- 每篇配**原创中文摘要** + **HBR 官方原文直链**（点「阅读原文」直达文章页，不再是列表）。

**功能**
- 搜索、分类筛选（含「新增」筛选）、按年份/标题排序、详情弹窗、绿色「新」角标、「最后更新」时间戳。

**持续运营**
- 「双周策展」自动化已上线，每两周五自动新增 3–5 篇真实文章并重部署。

**合规**
- 摘要原创 + 外链，无全文搬运，版权安全。

---

## 附：如何把这份复盘变成公众号内容

这份复盘本身就是极好的公众号素材。三种用法：

1. **直接发「做站复盘」**：用上面五段，标题可定为《我用 AI 搭了一个 HBR 经典阅读站，顺便想清楚了自己的公众号方向》。真实、有细节、有踩坑，读者爱看。
2. **拆成「经典解读」系列**：站里的 34 篇就是 34 期选题。每期一篇：一句话价值 + 我的解读 + 原文链接。这是能长期做、越做越有壁垒的 IP 主线。
3. **做「方法论」人设**：持续输出「我是怎么筛选 / 摘要 / 策展的」，强化你作为「管理学内容策展人」的标签。

> 下一步可选：需要我把任意一篇（如《什么是战略》《创新者的窘境》）扩写成一篇 1500 字公众号初稿吗？或者直接用公众号发布技能帮你排好版、配好封面发出去。
